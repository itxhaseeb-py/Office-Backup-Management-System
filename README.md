# Office-Backup-Management-System
A Python Office Backup Management System built by combining the `os`, `pathlib`, and `shutil` modules. This project automates folder inspection, backup creation, ZIP archiving, optional folder deletion, and generates a final backup report as part of my Python automation learning journey.
